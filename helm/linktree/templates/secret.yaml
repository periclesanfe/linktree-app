# Secret placeholder - deve ser criado manualmente ou via SealedSecret
# kubectl create secret generic linktree-secrets \
#   --from-literal=DB_USER=postgres \
#   --from-literal=DB_PASSWORD=your-password \
#   --from-literal=JWT_SECRET=your-jwt-secret \
#   -n dev
#
# kubectl create secret generic linktree-secrets \
#   --from-literal=DB_USER=postgres \
#   --from-literal=DB_PASSWORD=your-password \
#   --from-literal=JWT_SECRET=your-jwt-secret \
#   -n prod

# Uncomment para criar secret via Helm (NÃO RECOMENDADO para produção)
# apiVersion: v1
# kind: Secret
# metadata:
#   name: {{ .Values.secretName }}
#   labels:
#     {{- include "linktree.labels" . | nindent 4 }}
#   annotations:
#     "argocd.argoproj.io/sync-wave": "0"
# type: Opaque
# stringData:
#   DB_USER: "postgres"
#   DB_PASSWORD: "changeme"
#   JWT_SECRET: "changeme"
